export interface PeopleAlsoAskProps {
  className?: string;
}
export function PeopleAlsoAsk({ className }: PeopleAlsoAskProps) {
  return (
    <div className={`${className}`}>
      <h1>PeopleAlsoAsk works!</h1>
    </div>
  );
}
